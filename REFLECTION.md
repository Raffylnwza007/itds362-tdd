จำนวนเทสต์ โค้ดจริง และอัตราส่วน
ในโปรเจกต์นี้มี Unit Tests ทั้งหมด 6 ตัวในไฟล์ test_kitchen.py ความยาวรวมราว 45 บรรทัด ขณะที่โค้ดจริงใน kitchen.py มีความยาวประมาณ 30 บรรทัด ทำให้อัตราส่วนโค้ดทดสอบต่อโค้ดจริงอยู่ที่ประมาณ 1.5 : 1


ช่วงที่แดงนานที่สุดคือ ตอนทำ A5 ต่อเนื่องไป A6  ใน A5 พอเพิ่มพารามิเตอร์ unit เข้ามาใน Quantity เทสต์ก่อนหน้านี้พังแทบหมดเพราะเรียกใช้แบบส่งพารามิเตอร์ตัวเดียว ต้องมานั่งแก้เทสต์เก่าให้รองรับ จนได้เรียนรู้ว่าการทำ Factory functions อย่าง grams() กับ ounces() ช่วยลดการผูกมัดโค้ดได้เยอะมาก  ใน A6 ต้องปรับวิธีคิดใหม่ทั้งหมด ตอนแรกจะรีบคำนวณบวกเลขตรงๆ ใน plus() แต่โจทย์บังคับให้ใช้แนวคิด Expression และคลาส Sum มาคั่นกลาง กว่าจะเข้าใจว่า plus แค่จำเจตนาไว้แล้วค่อยให้ Converter มาสั่ง reduce ก็ทำให้ติดอยู่ตรงแถบสีแดงนานกว่าสเต็ปอื่น  
 
 การเลือกใช้ Fake It, Triangulate และ Obvious ImplementationFake It (A1 / A6): ตอนใส่ self.amount = 600 หรือ grams(500) รู้สึกขัดใจมากเพราะรู้อยู่เต็มอกว่ามันเป็นโค้ดที่ผิด แต่พอลองทำจริง มันช่วยให้เราโฟกัสแค่การเชื่อมต่อของระบบ (Wiring) และโครงสร้างคลาสให้เวิร์กก่อน โดยไม่ต้องเสียสมาธิไปพะวงกับตรรกะคำนวณ  Triangulate (A2): ตอนที่เพิ่มเคสคูณ 2 แล้วผลลัพธ์เป็น 400 การมีตัวอย่างสองตัวที่ผลลัพธ์ไม่ตรงกัน มันบีบให้ค่าคงที่เดิม (600) ใช้ต่อไม่ได้ ทำให้เราเปลี่ยนมาเขียนสูตรคำนวณจริง self.amount * multiplier ได้อย่างมั่นใจโดยมีหลักฐานคอยคุม  Obvious Implementation (A4): การใส่ __repr__ หรือการดึง self.unit ไปใส่ในผลลัพธ์รอบใหม่ ตรงนี้เขียนโค้ดจริงลงไปเลยตั้งแต่แรก เพราะสั้น ตรงไปตรงมา และไม่ซับซ้อนพอที่จะต้องถอยไป fake ทีละขั้น


  รายการที่ยังค้างอยู่ใน Test List☐ 200 g + 1 oz แปลงผลลัพธ์เป็นกรัมโดยใช้อัตราแปลงหน่วย  ☐ (200 g + 1 oz) × 2  อันที่จะทำต่อจริง: การบวกข้ามหน่วย 200 g + 1 oz เพราะนี่คือหัวใจของแอปสูตรอาหารตามโจทย์ ต้องไปต่อยอดที่ Converter ให้เก็บตารางอัตราแปลงหน่วย (Rate)  อันที่ควรตัดทิ้ง: (200 g + 1 oz) × 2 ควรถอดออกก่อนเพราะเป็นการคาดเดาล่วงหน้า (Over-engineering) ในชีวิตจริงเราแปลงและรวมวัตถุดิบให้เป็นหน่วยมาตรฐานให้เสร็จก่อนแล้วค่อยขยายสูตรทีเดียวก็ได้ ตราบใดที่ยังไม่มี requirement ระบุชัดเจน การเขียนดักไว้จะทำให้สถาปัตยกรรมของ Expression ซับซ้อนเกินจำเป็น

เมท็อด Quantity.reduce ถ้าออกแบบบนกระดาษก่อนจะเขียนไหม?
ตอบตามตรงคือไม่มีทางเขียนแน่นอน เพราะถ้าออกแบบล่วงหน้า เราคงมองว่า Quantity มันมีค่าของมันสมบูรณ์อยู่แล้ว จะต้องมาสั่ง "reduce" ตัวมันเองทำไม คงจะไปเขียนเช็กประเภทใน Converter แทน เช่น if isinstance(source, Sum): ... else: ...

แต่การที่ TDD พาเรามาจุดนี้ ทำให้ค้นพบว่าการมี Quantity.reduce ช่วยสร้างความเป็น Polymorphism ทำให้ทั้ง Quantity และ Sum มีหน้าตาภายนอกเหมือนกัน Converter แค่สั่ง .reduce() ได้เลยโดยไม่ต้องสนว่าข้างในเป็นนิพจน์ซ้อนกันกี่ชั้น ซึ่งทำให้โค้ดยืดหยุ่นและอ่านง่ายกว่ามาก